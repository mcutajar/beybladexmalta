# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'


PAYMENTS_ADMIN_PASSPHRASE=test-passphrase
TOURNAMENTS_ADMIN_PASSPHRASE=test-passphrase

# Deliberately not the production host: SeoMetadataTest and SitemapTest assert
# that every URL is absolute and shares one secure origin, and asserting that
# against the real domain would pass just as well if the value were hard-coded
# somewhere it should not be.
SITE_URL=https://league.test
