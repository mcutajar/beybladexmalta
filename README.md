# project in symfony docker

launch with: `docker compose --env-file .env.local -f compose.yaml up -d --build`

dev mode: `docker compose --env-file .env.local -f compose.override.yaml up -d --build`
