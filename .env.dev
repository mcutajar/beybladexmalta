
###> symfony/framework-bundle ###
APP_SECRET=19e05c0b95756e407c5e52a84c2f1e48
###< symfony/framework-bundle ###

###> canonical origin ###
# The dev stack answers on plain HTTP, and HTTP_PORT moves it per worktree, so
# this is only ever approximately right. Nothing in dev reads it but the
# canonical tags, which are not the point of a local page.
SITE_URL=http://localhost
###< canonical origin ###
